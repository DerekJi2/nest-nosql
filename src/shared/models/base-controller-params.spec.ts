import { BaseControllerParams } from './base-controller-params';

describe('BaseControllerParams', () => {
  it('should be defined', () => {
    expect(new BaseControllerParams()).toBeDefined();
  });
});
